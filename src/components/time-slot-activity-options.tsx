import { SavedActivity } from '@/db/types'

interface TimeSlotActivityOptionsProps {
  activity: SavedActivity
}

export function TimeSlotActivityOptions({
  activity,
}: TimeSlotActivityOptionsProps) {
  return <div>asfd</div>
}
